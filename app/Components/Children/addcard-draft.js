// this is placeholder javascript to handle the add card function

// this is placeholder javascript to handle the add card function

  // var passport = require ('../../../config/passport.js');
  // console.log("You found "+currentUserEmail);

  // load up the card model
  var Card = require('../../models/card');

  // // event listener for submit button click
  // $("#addCardBtn").on("click", function()
  // {
  //   var storeName = $("#storeNameInput").val().trim();
  //   // need to strip the dollar sign off the input amount
  //   var cardBalance = $("#cardBalanceInput").val().trim();
  //   var redeemCode = $("#redeemCodeInput").val().trim();
  //   var tradeAvailability = false;

  //   // clear form fields
  //   $("#storeNameInput").val("");
  //   $("#redeemCodeInput").val("");
  //   $("#cardBalanceInput").val("");

  //   // Don't refresh the page
  //   return false;
  // });

  // module.exports = function(addcard) {
  //     // get the card properties from the form in the Add Card component
  //   addcard.addNewCard(function(req, res){

      // var newCard            = new Card();
      //   // newCard.cardOwnerEmail  = currentUserEmail;
      //   newCard.storeName       = storeNameInput;
      //   newCard.cardBalance     = cardBalanceInput;
      //   newCard.redeemCode      = redeemCodeInput;
      //   newCard.tradeAvailability = false;

        // // save the new card
        // newCard.save(function(err) {
        //     if (err)
        //         throw err;
        //     return done(null, newCard);
        // });

  //   });

  // };